@using LightenUp.Web.Models
@{
    Layout = "_LayoutDashboard";
    ViewData["Title"] = "Riwayat Jadwal Pasien";

    var sessions = ViewBag.Sessions as List<Schedule> ?? new List<Schedule>();
    var patientId = (int)(ViewBag.PatientId ?? 0);
    var idCulture = new System.Globalization.CultureInfo("id-ID");
}

<a href="javascript:history.back()" class="back-link">← Kembali</a>

<div class="dashboard-header" style="margin-bottom: 20px;">
    <div>
        <h1 class="text-header">Riwayat Jadwal: @ViewBag.PatientName</h1>
        <p class="text-subtitle">Pantau seluruh riwayat konseling untuk pasien ini.</p>
    </div>
</div>

<div class="patient-list-container">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;">
        <a href="@Url.Action("AddSchedule", "Psychologist", new { patientId })" style="display: flex; align-items: center; gap: 8px; padding: 12px 20px; background: var(--primary-purple); color: white; border-radius: 12px; font-size: 14px; font-weight: 600; cursor: pointer; text-decoration:none;">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
            Tambah Jadwal Pasien
        </a>
    </div>

    @if (!sessions.Any())
    {
        <div style="padding: 40px; text-align: center; color: #9CA3AF; border: 1px dashed var(--border-color); border-radius: 12px;">
            Belum ada jadwal untuk pasien ini.
        </div>
    }
    else
    {
        <div style="display: flex; flex-direction: column; gap: 16px;">
            @{ int idx = 0; }
            @foreach (var s in sessions)
            {
                idx++;
                var displayStatus = s.Status == "Cancelled" ? "Dibatalkan"
                                  : s.Status == "Completed" ? "Selesai"
                                  : (DateTime.Now < s.SessionStart ? "Akan Datang" : "Selesai");
                <div class="history-card">
                    <div>
                        <h3>Konseling @idx - @displayStatus</h3>
                        <p>@s.SessionStart.ToString("d MMMM yyyy", idCulture)</p>
                        <p>@s.SessionStart.ToString("HH.mm") - @s.SessionStart.AddMinutes(s.DurationMinutes).ToString("HH.mm")</p>
                        @if (!string.IsNullOrEmpty(s.Notes))
                        {
                            <p style="font-size:12px; opacity:0.85;">@s.Notes</p>
                        }
                    </div>
                </div>
            }
        </div>
    }
</div>
