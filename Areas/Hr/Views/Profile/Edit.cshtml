@model LightenUp.Web.Models.ViewModels.HrProfileEditViewModel
@{
    ViewData["Title"] = "Edit Profil";
    ViewBag.ActiveNav = "Profil";
}
@* #Bagian EditProfilHR# *@

@section Head {
    <link rel="stylesheet" href="~/css/hr/profile-edit.css" asp-append-version="true" />
}

<a class="pd-back" asp-action="Index">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
    Profil Saya
</a>

<div class="hrpf-edit-wrap">
    <h2 class="hrpf-edit-title">Edit Profil HR</h2>
    <p class="hrpf-edit-sub">
        Perbarui informasi kontak dan data kepegawaian Anda.
    </p>

    <form asp-action="Edit" method="post" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="hrpf-edit-val-summary"></div>

        <div class="pd-card pd-card--plain hrpf-edit-card">
            <div class="pd-card-head">
                <div class="pd-card-icon hrpf-edit-card-icon--blue">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                </div>
                <h3 class="pd-card-title">Informasi Pribadi</h3>
            </div>
            <div class="pd-card-body">
                <div class="form-group">
                    <label asp-for="ProfilePicture" class="form-label">Foto Profil</label>
                    <input asp-for="ProfilePicture" type="file" accept="image/*" class="form-control hrpf-edit-file-input" />
                </div>
                <div class="form-group">
                    <label asp-for="FullName" class="form-label">Nama Lengkap</label>
                    <input asp-for="FullName" class="form-control" />
                    <span asp-validation-for="FullName" class="hrpf-edit-val-error"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Phone" class="form-label">Nomor Telepon</label>
                    <input asp-for="Phone" class="form-control" />
                </div>
            </div>
        </div>

        <div class="pd-card pd-card--plain hrpf-edit-card">
            <div class="pd-card-head">
                <div class="pd-card-icon hrpf-edit-card-icon--purple">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/><line x1="12" y1="12" x2="12" y2="16"/><line x1="10" y1="14" x2="14" y2="14"/></svg>
                </div>
                <h3 class="pd-card-title">Kepegawaian <small class="hrpf-edit-card-title-sub">— @Model.CompanyName</small></h3>
            </div>
            <div class="pd-card-body hrpf-edit-card-body-grid">
                <div class="form-group hrpf-edit-field">
                    <label asp-for="EmployeeId" class="form-label">Employee ID</label>
                    <input asp-for="EmployeeId" class="form-control" placeholder="Mis. HR001" />
                </div>
                <div class="form-group hrpf-edit-field">
                    <label asp-for="Department" class="form-label">Divisi (Opsional)</label>
                    <input asp-for="Department" class="form-control" />
                </div>
            </div>
        </div>



        <div class="hrpf-edit-actions">
            <a asp-action="Index" class="btn-outline hrpf-edit-btn-cancel">Batalkan</a>
            <button type="submit" class="btn-fill hrpf-edit-btn-save">Simpan Perubahan</button>
        </div>
    </form>
</div>
